# Production profile: use a managed PostgreSQL database and a reviewed schema.
spring.h2.console.enabled=false
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
survey.admin-cookie-secure=true
